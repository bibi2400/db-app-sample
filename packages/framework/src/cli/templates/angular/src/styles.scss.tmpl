@use '@angular/material' as mat;
@use '{{PKG}}/styles/theme' as eaf;

html {
  @include eaf.eaf-base(
    $primary: mat.$cyan-palette,
    $tertiary: mat.$orange-palette,
  );
}

@include eaf.eaf-severity-themes(
  $danger:  mat.$red-palette,
  $warning: mat.$orange-palette,
  $success: mat.$green-palette,
  $info:    mat.$azure-palette,
);

// Sfondo globale dell'app. Personalizza colore/immagine/overlay qui.
// Il mixin è zoom-aware: la dimensione del pattern viene compensata
// automaticamente con calc($size / --eaf-content-zoom).
//
// Esempio con pattern ripetuto + overlay (ancorato a sx/top, invariante allo zoom):
//   @include eaf.eaf-background(
//     $color: #f5f7fa,
//     $image: url('/assets/bg-pattern.png'),
//     $overlay: rgba(245, 247, 250, 0.7),
//     $size: 600px,
//     $repeat: repeat,
//   );
@include eaf.eaf-background($color: #f5f7fa);

body {
  color-scheme: light;
  color: var(--mat-sys-on-surface);
  font: var(--mat-sys-body-medium);
  margin: 0;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
    Oxygen, Ubuntu, Cantarell, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  color: #1a1d29;
}

html, body {
  height: 100%;
  margin: 0;
}

.material-symbols-outlined {
  font-variation-settings:
    'FILL' 0,
    'wght' 400,
    'GRAD' 0,
    'opsz' 24;
}
